// 리터럴 표현식
var score=100;
var grettings='hello!';

// 식별자 표현식 (선언이 이미 존재한다고 가정)
sum
person.name
arr[1]

// // 연산자 표현식
10+20;
sum = 10;
sum !== 10;

// //함수/메서드 호출 표현식(선언이 이미 존재한다고 가정)
square();
person.getName();


// 변수 선언문
var x;

//할당문
x=5;

//함수 선언문
function foo(){
  console.log(x);
}

//조건문
if (x > 1) {
  console.log(x);
}

//반복문
for (var i=0; i<2; i++){
  console.log(i);
}